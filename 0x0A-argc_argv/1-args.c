#include "main.h"
#include <stdio.h>

/**
 * main - a program that prints the number of arguements passed into it
 * @argc: argument counter
 * @argv: argument vertor, an array
 * Return: Always 0 (Success)
 */

int main(int argc, __attribute__ ((unused)) char *argv[])
{
	printf("%d\n", argc - 1)

		return (0);
}

